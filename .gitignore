# --- Secrets: never commit ---
.env
.env.*
!.env.example

# Personal, machine-local overrides (NOT shared with the team)
.claude/settings.local.json
.codex/config.local.toml

# Linked git worktrees created inside the repo (e.g. parallel agent worktrees)
.claude/worktrees/

# --- Dependencies / build output ---
node_modules/
dist/
build/
.next/
out/
coverage/
*.log

# --- Editor / OS cruft ---
.DS_Store
Thumbs.db
.idea/
*.swp

# --- Next.js / tooling ---
.pnp
.pnp.*
*.tsbuildinfo
next-env.d.ts
.vercel
*.pem

.agent